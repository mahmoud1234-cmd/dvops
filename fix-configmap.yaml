apiVersion: v1
kind: ConfigMap
metadata:
  name: student-app-config
  namespace: devops
data:
  spring.datasource.url: jdbc:mysql://mysql-service:3306/studentdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
  spring.datasource.username: student
  spring.datasource.password: student123
  spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver
  spring.jpa.hibernate.ddl-auto: update
  spring.jpa.show-sql: "true"
  # REMOVE spring.profiles.active OR set to default
  # spring.profiles.active: default
